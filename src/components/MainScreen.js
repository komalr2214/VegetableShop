import React from 'react';

function MainScreen() {
    return (
        <div className="flex items-center justify-center bg-red-300 h-screen overflow-hidden">
            <h1 className="text-4xl font-bold">Vegetable Shop</h1>
        </div>
    );
}

export default MainScreen;
